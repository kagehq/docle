{
  "name": "docle-playground",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --port 3001",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "clean": "rm -rf .nuxt .output node_modules/.cache",
    "fresh": "npm run clean && npm install && npm run dev"
  },
  "dependencies": {
    "nuxt": "^3.13.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.12.0",
    "tailwindcss": "^3.4.0"
  }
}

